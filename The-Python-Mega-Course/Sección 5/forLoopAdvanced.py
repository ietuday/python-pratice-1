names = ['james', 'josh', 'jack']

email_domains = ['gmail', 'hotmail', 'yahoo']


for i,j in zip(names, email_domains):
    print(i,j)


'''
there's a difference between + and , when using str variables.
if we use + with 2 strings, the output will be the 2 strings without a white space
and if we use , there will be a white space between them
'''
