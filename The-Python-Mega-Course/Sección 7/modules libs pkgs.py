'''
To import sthg:
import 'what we want to import'

all the libs, modules and packages we can import, are located at Lib folder
on python folder

module: single file
libraries are a collections of .py files
packages. third-party libraries

To install a third-party package, we use the following command:
pip install 'package we want to intall'

glob2.glob("*")
retireves all the directories from the current path

glob2.glob("*.txt")
retireves only .txt files from the current path
'''

#module
import os

#generate a list of file on the current folder
os.listdir()

#shows the
